import styled from 'styled-components';
import { Box } from '@material-ui/core';

const Container = styled(Box)`
  padding: 0;
  margin: 0;
  min-height: 100vh;
  border-radius: 0;
`;

export { Container };
